{
    "name": "tools",
    "id": "tools",
    "version": "1.10.0",
    "description": "A minimal dev container feature that bundles terminal-based development tools, AI coding CLIs, and 1Password CLI.",
    "options": {
        "installLazygit": {
            "type": "boolean",
            "description": "Install lazygit terminal UI for Git",
            "default": true
        },
        "installNvim": {
            "type": "boolean",
            "description": "Install neovim and supporting tools (ripgrep, fd, fzf)",
            "default": true
        },
        "lazygitVersion": {
            "type": "string",
            "description": "lazygit version to install (e.g., '0.40.2', 'latest')",
            "default": "latest"
        },
        "nvimVersion": {
            "type": "string",
            "description": "neovim version to install (e.g., '0.9.5', 'latest')",
            "default": "latest"
        },
        "installClaudeCode": {
            "type": "boolean",
            "description": "Install Claude Code CLI (AI coding assistant)",
            "default": true
        },
        "installCodex": {
            "type": "boolean",
            "description": "Install OpenAI Codex CLI",
            "default": false
        },
        "installGrok": {
            "type": "boolean",
            "description": "Install Grok CLI",
            "default": true
        },
        "installCursor": {
            "type": "boolean",
            "description": "Install Cursor Agent CLI",
            "default": true
        },
        "installGh": {
            "type": "boolean",
            "description": "Install GitHub CLI (gh)",
            "default": true
        },
        "installOp": {
            "type": "boolean",
            "description": "Install 1Password CLI (op)",
            "default": true
        },
        "installFdsx": {
            "type": "boolean",
            "description": "Install fdsx (fast data serialization tool)",
            "default": true
        },
        "installRtk": {
            "type": "boolean",
            "description": "Install rtk (Rust Token Killer - token-optimized CLI proxy)",
            "default": true
        },
        "installWitr": {
            "type": "boolean",
            "description": "Install witr process tracing CLI and TUI",
            "default": true
        },
        "installPi": {
            "type": "boolean",
            "description": "Install pi coding agent",
            "default": true
        },
        "installOhMyPi": {
            "type": "boolean",
            "description": "Install Oh My Pi coding agent",
            "default": true
        }
    }
}
